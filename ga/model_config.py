class ModelConfig:
    def __init__(self, feature_inputs, arg_outputs, size, num_functions, model_format, scope):
        self.feature_inputs = feature_inputs
        self.arg_outputs = arg_outputs
        self.size = size
        self.model_format = model_format
        self.scope = scope
        self.num_functions = num_functions
